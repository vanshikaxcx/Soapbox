"""Application use cases and typed ports. Depends on services.domain; adapters
depend on this, never the reverse. Ports are reviewed by the adapter owner
(POA ownership table: services/application/** — use-case owner; ports reviewed
by adapter owner).
"""
