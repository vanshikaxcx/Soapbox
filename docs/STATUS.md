# ProofPath package status

This is the canonical work-package status ledger. Update it only through the approved package workflow; links to implementation PRs/commits and verification evidence are added when available.

| WP | Owner | Dependency state | Specification | Package status | Implementation PR/commit | Verification | Blockers |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WP-00 | P4 | None | [SPEC APPROVED](specs/WP-00-repository-toolchain-contract-baseline.md) | **Stage 8 complete — ready to merge with one documented exception** | [PR #1](https://github.com/vanshikaxcx/Soapbox/pull/1) (draft) | Stages 1–7 complete per prior entries (see `docs/evidence/gate-a-clean-clone.md` and `LEARNING.md` for full defect history). **First real GitHub Actions run (2026-09-17)** found and fixed a genuine checkout-ordering defect in `checks.yml` (see `LEARNING.md`); 4 of 6 jobs now pass in real CI. **`build`/`web-smoke` fail deterministically** on unauthenticated `public.ecr.aws` pulls being rate-limited on GitHub-hosted runners' shared egress IPs — this is **AC-00-22, the one AC not met**, and is a real environmental gap tracked to close via WP-01's AWS OIDC/ECR-authentication work, not a WP-00 code defect. **Stage 8 (2026-09-17): all 28 acceptance criteria mapped to evidence — 27 PASS, 1 (AC-00-22) explicitly not met and documented, 1 (AC-00-26) correctly N/A.** Final `verify-gate-a` at the committed Stage 8 tip: full green pass, exit 0, clean tracked tree. One comprehensive full-diff `/code-review` pass found 8 findings (0 HIGH, 4 MEDIUM, 3 LOW, all independently re-verified by reading/running the actual code); 4 mechanical, no-new-behavior fixes applied and re-verified (`Invoke-OpenApiCheck` path fragility, `Get-SemanticVersion` uncaught-throw guard, Vite `strictPort`, CODEOWNERS gaps for `docs/PROOFPATH-SPEC.md`/`security/suppressions.toml`/shared `services/application`+`tests/unit\|contracts\|integration` paths); 3 deliberately deferred (version-pin dedup across `checks.yml`/`proofpath.ps1` — a real fix is new behavior, which Stage 8's own spec text excludes; 3-way regex YAML parsing duplication; `.gitignore` fixture carve-out — no fixture convention exists yet to carve out). Full detail in `LEARNING.md`'s Stage 8 entries. | **Merge decision (2026-09-17, explicit user call):** merge WP-00 now with `build`/`web-smoke` as a known, documented exception rather than blocking on WP-01 — `checks.yml` runs unconditionally on every future PR, so gating on these two would block the entire team, not just WP-00. Requires a temporary branch-protection adjustment (drop `build`/`web-smoke` from required checks, or an admin-override merge) since this is a genuine exception to Gate C's "all required checks pass." Restore full required-checks status once WP-01 lands ECR authentication. Stage 6 reviewer sign-off remains internal `/code-review` per earlier explicit user decision (P1/P2 formally skipped). |
| WP-01 | P4 | WP-00 merged — satisfied | [Finalized by owner, spec-review sign-off waived](specs/WP-01-cloud-viability-checkpoint.md) | Implementing — AC-01-08, AC-01-01/03/05/02 slices built locally; P1 deploying/testing on her machine (see HANDOFF-WP01-DEPLOY.md), verification results pending | — | Not started (deployment) | **2026-09-19: prior account-verification blocker resolved by switching AWS accounts** (Sparsh's `651706736092` → P1's `780891108112`, `voxmind-shared-test`), not by the original account's hold clearing — that account's status remains unconfirmed and should not be assumed cleared if reused. Region unchanged (`ap-south-1`). Model choice resolved to Amazon Nova Pro (`amazon.nova-pro-v1:0`), confirmed live-invokable, no cross-region tag. **Open follow-up:** budget/cost tracking (Open questions item 7) still needs to be re-confirmed against the new account — the hackathon's $100 credit lives on the old account, not P1's; Polly/Transcribe availability not yet re-glanced on P1's account specifically. Tested locality (110001) carries an unverified IP-geolocation-coincidence risk flagged in the spec, not yet closed. **AC-01-08 progress (2026-09-19):** `infra/github-oidc.yaml` (bootstrap OIDC provider + least-privilege `ecr-public:GetAuthorizationToken`-only role) and `checks.yml`'s `build`/`web-smoke` OIDC-auth steps are written; see `LEARNING.md`'s 2026-09-19 entry. **Not yet deployed** — no AWS CLI/credentials were available in the authoring environment, so the template must be deployed by whoever has console/CLI access to P1's account, the resulting role ARN set as the `PROOFPATH_CI_ECR_ROLE_ARN` repository variable, and two consecutive first-attempt-pass CI runs observed before AC-01-08 can be marked satisfied. **AC-01-01/AC-01-03 slice progress (2026-09-19):** the one disposable checkpoint record's port/adapters/API handler are built and tested locally (602 tests passing; see `LEARNING.md`), `infra/template.yaml` extended with `AppTable` (DynamoDB, Streams enabled), Cognito User Pool/Client, and least-privilege checkpoint Lambda IAM — structurally verified via `sam validate`/`sam build`, not yet deployed. **Cognito-authorizer gap resolved (2026-09-19, structurally):** `/checkpoints/{checkpointId}` now carries a real `x-amazon-apigateway-authorizer` (`cognito_user_pools`) extension authored directly in `contracts/openapi.yaml`, bound to `CheckpointUserPool`'s ARN; `infra/template.yaml`'s `ProofPathApi` switched from `DefinitionUri` to `DefinitionBody: Fn::Transform: AWS::Include` so that `Fn::GetAtt` resolves as an ordinary CloudFormation intrinsic once inlined — SAM's own `Auth` property remains confirmed non-viable with an externally-sourced API definition, per the (now-updated) investigation comment on `ProofPathApi`. `services/api/checkpoint.py`'s `_resolve_owner_from_cognito_claims` was already correct (fails closed with 500 if `requestContext.authorizer.claims` is absent — never trusts a client-supplied owner ID) and needed no change. Verified locally: `sam validate --lint` passes, `sam build` passes, `openapi-check` shows zero generated-client drift, and 4 new deterministic contract tests in `tests/contracts/test_checkpoint_api_contract.py` cover the security requirement, the authorizer's real Cognito binding, `/health`'s continued exemption, and the fail-closed owner resolution (25/25 `tests/contracts` pass; `mypy`/`ruff` clean). **Still unverified without a real deploy:** that API Gateway actually rejects unauthenticated/cross-owner requests and actually populates `requestContext.authorizer.claims` as assumed — this is exactly AC-01-03's remaining test-plan item, now unblocked for P1 to exercise. `docs/evidence/checkpoint.md` still exists only as an unfilled evidence skeleton. **Coordination note:** P2's WP-04 branch (PR #5, open) already has a working Playwright/agent container P2 owns; WP-01's Fargate task should reuse it rather than duplicate it — not yet added to the template. **AC-01-05 slice progress (2026-09-19):** the durable job chain (checkpoint job/outbox model, DynamoDB adapter with `transact_write_items`, publisher/controller/task/trigger Lambdas, SQS+DLQ, EventBridge rule, Step Functions Standard state machine) is built and tested locally (631 tests total); see `LEARNING.md`. **Real repo-wide Lambda packaging bug found and fixed along the way** (not WP-01-specific — affects any future Lambda needing `services.application`/`services.domain` imports): confirmed via real `sam local invoke` that a function-subdirectory `CodeUri` breaks `services.*` imports at runtime; a repository-root `CodeUri` was tried and rejected after `.samignore` was confirmed non-functional with this pinned SAM CLI (real risk of exceeding Lambda's package size limit, not just slow); fixed via `uv build --wheel` + `uv pip install --target ... --no-deps` injecting `services/**` into each function's build output post-`sam build`, added to `scripts/proofpath.ps1`. **AC-01-02 progress (2026-09-19):** Bedrock (`converse()`)/Polly/Transcribe-streaming adapters built and wired into `CheckpointTaskFunction` (recording request IDs/event counts onto the job record), tested locally including a direct `docker run` check that the new `amazon-transcribe`/`awscrt` native dependency actually imports inside the real Lambda runtime container — not yet deployed; whether synthetic audio reliably triggers a real Transcribe event is explicitly flagged as unverifiable from this environment. **AC-01-06 progress (2026-09-19):** `infra/template.yaml` gained `AmplifyApp`/`AmplifyBranch` (GitHub-sourced, `vanshikaxcx/Soapbox`, building `web/` with the same `npm ci && npm run build` steps `scripts/proofpath.ps1 build` already runs). No IAM service role is attached to `AmplifyApp` — the subagent's initial draft attached AWS's broad `AdministratorAccess-Amplify` managed policy via a new `AmplifyServiceRole`, which this review removed as a real AC-01-13 (least-privilege) violation: a static Vite build needs no service role at all, so the fix was to drop the role entirely rather than narrow it, and `CheckpointUserPoolClient`'s `CallbackURLs`/`LogoutURLs` now derive the real Amplify domain via `Fn::Sub` over `AmplifyApp.DefaultDomain` (real CloudFormation intrinsic, not a placeholder) alongside the existing `http://localhost:5173/` local-dev entry. CORS is wired as a real single-origin allowlist (never a wildcard): `contracts/openapi.yaml` gained an `OPTIONS` MOCK-integration preflight route answering only the deployed Amplify origin, and `services/api/checkpoint.py`'s new `with_cors`/`_cors_allowed_origin` add `Access-Control-Allow-Origin` to the GET/PUT Lambdas' actual responses (necessary because Lambda proxy integration responses bypass API Gateway's own integration-response header mapping), reading the allowed origin from a new `PROOFPATH_ALLOWED_ORIGIN` env var the template sets from the same Amplify-domain intrinsic. The GitHub PAT for Amplify's source connection is read at deploy time via an `{{resolve:ssm-secure:...}}` dynamic reference (parameter name `AmplifyGitHubAccessTokenSsmParameterName`, default `/proofpath/amplify-github-access-token`) — never a literal value in the template, a parameter default, or a CLI arg. Verified locally: `sam validate --lint` passes, `sam build` (containerized) passes end to end, `openapi-check`'s validation/contract-test/regeneration steps all pass (the script's own drift check only fails because the regenerated `web/src/api/generated/schema.d.ts` is legitimately uncommitted in this sandbox — two consecutive regenerations produced byte-identical output), and 12/12 `tests/unit/test_checkpoint_api.py` (3 new CORS-wrapper tests added) plus the full 89-test `tests/unit`+`tests/contracts` suite pass. **Explicitly unresolved, flagged rather than papered over (no live AWS/GitHub access in this authoring session):** (1) whether `AWS::Amplify::App`'s `AccessToken` PAT-based GitHub connection actually succeeds against the real `vanshikaxcx/Soapbox` repo, versus needing the console's interactive GitHub-App-installation flow instead — untested; (2) whether Amplify transforms a branch name containing `/` (e.g. this WP-01 branch) when building the `*.amplifyapp.com` subdomain, which would make the `Fn::Sub`-constructed Cognito/CORS origin wrong until corrected against the real deployed domain — `AmplifyBranchName` defaults to `main` to sidestep this pending verification, override only after checking. (3) `web/` itself has no PKCE sign-in flow or API-base-URL/Cognito-client-ID wiring yet (`web/.env.example` only defines `VITE_PROOFPATH_MODE`) — AC-01-06's actual browser-driven round trip cannot be exercised until P1 builds that in `web/**`, which is out of this task's ownership boundary and was not attempted here. **AC-01-07 progress (2026-09-19):** `infra/search.yaml` authored as a separate, independently deployable template per the spec's isolation rationale — one `AWS::OpenSearchService::Domain` (`t3.small.search` x1, no dedicated master, encryption at rest/in transit and enforced HTTPS all enabled, access policy scoped to exactly one required `CheckpointTaskExecutionRoleArn` parameter, no anonymous/`*` fallback); no real index created, matching WP-01's own out-of-scope note. `scripts/proofpath.ps1`'s `openapi-check` step extended to also `sam validate` this template (no new top-level command). Provisioning outcome recorded honestly in `docs/evidence/checkpoint.md` as an **exact blocker, not a success**: no AWS credentials in this authoring environment, so the domain has never actually been deployed or access-checked — P1's exact next steps (deploy with the real execution-role ARN, run one signed access check, tear down per AC-01-09) are documented there. The canonical-fallback path AC-01-07 also requires was genuinely exercised: 2 new passing tests in `tests/contracts/test_checkpoint_canonical_fallback_ac_01_07.py` retrieve a checkpoint record by owner through `CheckpointStatePort` against the in-memory adapter with zero OpenSearch import anywhere in the module — not merely asserted equivalent to AC-01-03's round trip. AC-01-09 (budget/teardown) is not yet started. **Preliminary code-review fix pass (2026-09-19, ahead of the final mandated WP-01 review):** 4 parallel `/code-review`-driven reviews (correctness/idempotency, security/IAM/CORS, contracts/tests/ownership, docs/cost/observability) found and confirmed 4 HIGH-severity real bugs plus several MEDIUM/LOW gaps, all independently re-verified (not just accepted from subagent reports) and fixed: (1) `services/api/checkpoint.py`'s GET/PUT handlers had zero logging on internal errors and treated malformed requests as generic 500s instead of typed 4xx — fixed with request-id-correlated logging (never logging the raw event, which may carry an Authorization header) and a proper 422 for malformed input, while keeping the fail-closed 500 on absent authorizer claims unchanged; (2) `DynamoDbCheckpointState`'s conditional-write conflict handling returned a false `VersionConflict` for a same-key idempotent retry racing itself instead of replaying the original success, now fixed to match `InMemoryCheckpointState`'s reference behavior; (3) `checkpoint_task.py`'s duplicate-delivery guard could let two concurrent invocations both fire live Bedrock/Polly/Transcribe calls before either persisted a terminal status — fixed with an atomic conditional-write claim (`claim_task_execution`); (4) `controller.py` could permanently strand a job in `RUNNING` after an ambiguous crash-before-dispatch with no execution ever found — fixed with a symmetric atomic claim (`claim_redispatch`) that now actually redispatches instead of silently no-op'ing. Also fixed: `infra/github-oidc.yaml`'s pull_request OIDC trust condition was scoped by event type only (any PR, any branch) — split into two trust statements and added a `base_ref: main` condition on the pull_request one (GitHub's `pull_request` token has no branch info on `:sub` itself, confirmed); API Gateway's Cognito-authorizer 401/403 rejections bypassed the Lambda's CORS wrapper entirely, leaving the browser with an opaque CORS failure instead of the real denial — fixed with two raw `AWS::ApiGateway::GatewayResponse` resources (`UNAUTHORIZED`/`DEFAULT_4XX`) carrying the same single-origin allowlist (SAM's `GatewayResponses` property was tried first and confirmed to reproduce the same `DefinitionBody`/`Fn::Transform` merge bug already documented for SAM's `Auth` property); and a stale docstring in `checkpoint.py` overstating that the Cognito authorizer validates audience/scopes was corrected — it doesn't (a genuine, honestly-documented limitation of Cognito User Pool authorizers at the API Gateway layer, not fixable without a custom Lambda authorizer, which is out of WP-01 scope). All fixes independently re-verified: `sam validate` passes on both `infra/template.yaml` and `infra/github-oidc.yaml` (`--lint` reports one confirmed cfn-lint false positive, W3660, on the new GatewayResponse resources — a documented, accepted warning, not a structural error); 111/111 `tests/unit`+`tests/contracts`+`tests/integration` pass; `mypy` clean across 84 files; `ruff check`/`ruff format --check` clean. **This was a preliminary pass, not the final mandated WP-01 review** (see HANDOFF.md) — that full pass still happens once AC-01-04/09/10 land. **Also found:** `services/domain/*_test.py` (WP-02's own tests) are not actually run by `test`/`verify-gate-a` — only `tests/unit`/`tests/contracts`/`tests/integration` are, a pre-existing gap not fixed here since it's cross-owner. **AC-01-08 exception (2026-09-19, investigated and closed as not fixable from this project):** PR #9 (`fix/wp-01-ci-ecr-oidc-p4`) fixed three real, independently-confirmed bugs in the OIDC CI-auth path in sequence — an invalid 39-character `ThumbprintList` entry, a trust-policy `:sub` condition missing the numeric org/repo IDs this account's GitHub org includes in real tokens (`repo:vanshikaxcx@191361411/Soapbox@1370358227:...`, confirmed via a real token decode), and an implicit rather than explicit `audience` on `configure-aws-credentials`. After all three fixes were deployed and independently verified byte-for-byte correct against real token claims (`sub`, `base_ref`, `aud` all match the trust policy exactly), `build`/`web-smoke` still fail `AssumeRoleWithWebIdentity` with `AccessDenied`. Root cause: this AWS account (`780891108112`) is a member of an AWS Organization (`o-ywhgm7dmmo`) with Service Control Policies enabled, managed by a separate account (`339712945258`) this project has no access or standing to inspect or change — it is a company account used with permission for this project, not one under this team's administrative control. **Decision: `build`/`web-smoke` remain a documented, accepted exception for the rest of this hackathon**, the same pattern this row already used for the original AC-00-22 rate-limit gap — not something to keep chasing. The three OIDC bugs found along the way are real fixes worth keeping (they would matter immediately in an AWS account without this org-level restriction). **AC-01-04 blocker found (2026-09-20, flagged for P2, not fixed here — outside this task's ownership):** building P2's `services/agent` container (`docker build --platform linux/amd64 -f services/agent/Dockerfile .`) fails during `pip install -r services/agent/requirements.txt` — `websockets==17.1` requires Python ≥3.11, but pip cannot resolve it against the pinned base image `mcr.microsoft.com/playwright/python:v1.49.0-jammy`, indicating that image's Python is older than 3.11. This blocks AC-01-04's Fargate/Playwright locality-smoke test until P2 either bumps the base image or relaxes the `websockets` pin. Independently, the same build attempt also hit a local Docker Desktop failure (`write .../metadata_v2.db: read-only file system`) traced to the authoring machine's host disk being at 99% capacity — an environment issue, not a repo defect; retry pending disk cleanup. **AC-01-04 update (2026-09-20, P4/Sparsh):** P2's blocker above is resolved (PR #5 merged, then `fix/wp-04-blinkit-not-serviceable-p2`/`feat/wp-04-location-completeness-marker-p2` merged as PR #14/#15) and the Fargate task now runs successfully end to end. Running it surfaced a new, external blocker: Blinkit's Cloudflare edge blanket-blocks `ap-south-1` egress (confirmed across 5 separate Fargate tasks/IPs) — not a code or deploy defect. P4 accepted off-Fargate evidence (same fixed connector code, clearly labeled non-Fargate environment) as satisfying this gate's substantive intent; full reasoning and evidence tables are in `docs/evidence/checkpoint.md`'s AC-01-04/AC-01-10 sections. Overall WP-01 decision revised to **conditional pass, narrower than before** — see `checkpoint.md` for the complete gate-by-gate breakdown. Zepto's location is honestly labeled `unverified` rather than silently claiming a match; any demo/README claim about "two live merchants" should state this asymmetry (Blinkit verified-locality, Zepto live-but-unverified-locality) rather than presenting both as symmetric. |
| WP-02 | P3 | Blocked by WP-00 contracts | Not yet created | Draft | — | Not started | WP-00 |
| WP-03 | P1 | WP-00 (merged) | [Draft](specs/WP-03-web-shell-auth-client.md) | **Implementation complete on `feat/wp-03-web-client-p1`, rebased onto main; spec review requested** | — (branch pushed; PR not yet opened) | `npm run typecheck`/`lint`/`test` all pass (10 files / 65 tests); `npm audit`: 0 known vulnerabilities (react-router-dom bumped 7.1.1→7.18.4, closing 3 high-severity CVEs). Manually verified: real Cognito PKCE redirect from every route including `/` (confirmed against the real hosted-UI domain, which returned a genuine "Invalid client id" — proving PKCE URL construction, not a stub); keyboard-only path with visible focus; mobile viewport (375×812) layout; `/demo`'s async-state picker via real keyboard interaction. | WP-01 (real Cognito pool) for the one remaining acceptance criterion (authenticated health call against a **deployed** API) — otherwise unblocked and ready for P3's spec review. |
| WP-04 | P2 | Blocked by WP-00; checkpoint access | Not yet created | Draft | — | Not started | WP-00, WP-01 access gate |
| WP-05 | P1 | Blocked by WP-03; extraction contract | Not yet created | Draft | — | Not started | WP-03 |
| WP-06 | P2 | Blocked by WP-02, WP-04 | [SPEC](specs/WP-06-search-comparison-basket-repair.md) | Implementing | — | Not started | WP-02, WP-04 |
| WP-07 | P4 | Blocked by WP-01, WP-02 | Not yet created | Draft | — | Not started | WP-01, WP-02 |
| WP-08 | P3 | Blocked by WP-02, WP-06, WP-07 | Not yet created | Draft | — | Not started | WP-02, WP-06, WP-07 |
| WP-09 | P3 | Blocked by WP-07, WP-08 | Not yet created | Draft | — | Not started | WP-07, WP-08 |
| WP-10 | P4 | Blocked by WP-09 | Not yet created | Draft | — | Not started | WP-09 |
| WP-11 | P1 | Blocked by WP-05, WP-06, WP-08–10 | Not yet created | Draft | — | Not started | WP-05, WP-06, WP-08–10 |
| WP-12 | P4 | Blocked by all prior packages | Not yet created | Draft | — | Not started | Prior WPs |
